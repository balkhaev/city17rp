core.config = {}

core.config.defaults = {
  role = "citizen",
  access = {"settings"},
  money = 1000
}

core.config.settings = {
  spawnTime = 10,
  chatRadius = 1000,
  voiceRadius = 1000
}

core.config.zombie = {
  minLimit = 5,
  maxLimit = 60,
  eventLimit = 30,
  spawnTime = 10,
  eventSpawnTime = 5
}
